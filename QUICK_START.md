# 🚀 شروع سریع — Telegram Bot Pro v4.0

سه گام تا اجرای ربات.

---

## گام ۱ — توکن بگیرید

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید.
2. `/newbot` را بفرستید و نام و یوزرنیم ربات را وارد کنید.
3. توکنی که می‌دهد را کپی کنید (مثل `123456789:ABC...`).

---

## گام ۲ — تنظیم

```bash
cp .env.example .env
```

فایل `.env` را باز کنید و توکن را جایگزین کنید:

```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHI...
```

---

## گام ۳ — نصب و اجرا

```bash
bash SETUP.sh                              # نصب وابستگی‌ها
python3 telegram_turbo_pro_final.py        # اجرا
```

اگر همه‌چیز درست باشد، این پیام را می‌بینید:

```
🚀 TELEGRAM BOT Pro v4.0 فعال است!
🌐 پروکسی: غیرفعال (اتصال مستقیم)
📡 Polling شروع...
✅ ربات آماده است
```

حالا در تلگرام به ربات `/start` بفرستید.

---

## استفاده

| منو | کارها |
|-----|-------|
| 📋 کانال‌ها | افزودن، فهرست، حذف کانال |
| 🔧 پروکسی | افزودن، فهرست، حذف پروکسی |
| ❓ راهنما | توضیح کوتاه |

- **لغو عملیات:** هر زمان `/start` را بفرستید.
- **نام کانال معتبر:** ۵ تا ۳۲ کاراکتر انگلیسی/عدد/`_`.
- **پروکسی معتبر:** `1.2.3.4:8080` یا `http://host:8080` یا `socks5://host:1080`.

---

## رفع اشکال

| مشکل | راه‌حل |
|------|--------|
| `❌ توکن تنظیم نشده` | فایل `.env` را بسازید و توکن را وارد کنید |
| `ModuleNotFoundError` | `pip install -r requirements.txt` |
| ربات پاسخ نمی‌دهد | ربات باید در حال اجرا باشد؛ لاگ‌ها را در `logs/` ببینید |
| توکن لو رفت | با `/revoke` در @BotFather باطل و توکن تازه بگیرید |

---

## تست

```bash
TELEGRAM_BOT_TOKEN=x python3 test_bot.py
```

باید ببینید: `نتیجه: 56 پاس ✅ | 0 ناموفق ❌`
